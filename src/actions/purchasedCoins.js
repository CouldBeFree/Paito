import { PURCHASED_COINS } from './types';
import axios from 'axios';

// Get coins
/*export const getCoins = () =>{
    axios.get('https://min-api.cryptocompare.com/data/generateAvg?fsym=BTC&tsym=&e=Kraken')
        .then(res => {
            console.log(res.data)
        })
        .catch(function (error) {
            console.log(error);
        });
};*/
